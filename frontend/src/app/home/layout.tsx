import MinervaFoodsLayout from '@/components/layout/MinervaFoodsLayout';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <MinervaFoodsLayout>{children}</MinervaFoodsLayout>;
}